import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartsensordesign.smartfishfinder',
  appName: 'Smart Fish Finder',
  webDir: 'www',
};

export default config;
